{
  "buildCommand": "hugo --gc --minify",
  "outputDirectory": "public",
  "framework": "hugo",
  "builds": [
    {
      "src": "config.toml",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "public"
      }
    }
  ]
} 